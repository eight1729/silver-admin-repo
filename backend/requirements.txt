fastapi
uvicorn[standard]
sqlalchemy[asyncio]>=2.0
# PostgreSQL is the single database everywhere (local, tests, production).
psycopg[binary]
pydantic-settings
httpx
# Calls the external business API with a Cloud Run ID token
# (app/adapter/external_business_http.py).
google-auth
# google.auth.transport.requests raises ImportError without requests, which
# google-auth does not depend on itself.
requests
PyJWT[crypto]>=2.8
